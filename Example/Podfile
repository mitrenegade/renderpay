use_frameworks!

target 'RenderPay_Example' do
  pod 'RenderPay', :path => '../'
  pod 'RenderCloud', :git => 'https://bitbucket.org/renderapps/RenderCloud', :tag=>'2.0.0'
#  pod 'RenderCloud', :path=>'../../rendercloud'
  pod 'Stripe', '~> 14.0'

  target 'RenderPay_Tests' do
    inherit! :search_paths    
  end
end
